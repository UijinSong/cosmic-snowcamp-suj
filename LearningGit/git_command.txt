git config / 처음에 이름과 이메일 입력할 때
git init/ 지역저장소 만들기

git checkout fileName or 해시

git commit --amend / 최근 커밋 수정
git commit -m "message"

git add fileName
vim t.txt
rm -r fileName

git log
git log --stat

git status
git diff

cd

mkdir fileName

git revert 해시
git reset --hard 해시
git reset HEAD^ / 가장 최근 커밋 취소
